---
description: 消息应对与自我成长工作流 — 收到消息后的标准处理流程
alwaysApply: true
---


## 时间感知

分析消息时必须建立时间维度的判断：

1. **确认当前时间**：基于系统日期和最新同步时间，明确"现在是几点"
2. **计算消息间隔**：对方最后一条消息距今过了多久，用户最后一条消息距今过了多久
3. **结合时间背景分析**：
   - 对方的沉默是"正常间隔"还是"异常久没回"，要结合她平时的回复节奏判断
   - 当前时段（早/中/晚/深夜）影响消息策略——同样的内容在不同时间段发出效果不同
   - 对方当天的行程和状态（如刚旅行到校、在复习、已休息）会影响回复预期
4. **在分析中显式体现时间**：意图分析和策略方向中要写明时间因素如何影响了判断，而不是只分析文字内容



# 消息应对工作流

当用户说"收到消息了"、"帮我看看怎么回"或类似触发词时，按以下流程执行。

所有结构化输出写入 `workbench.md`，用户通过 markdown 预览查看和交互。

## 核心原则

在一个具体的沟通场景中，当对方的性格、关系阶段、情绪状态都是已知的，存在一个最优解。应该像顶级导师一样给出一个明确的最佳判断，而不是列出多个方案让用户自己选。

## 先判断"要不要"，再讨论"怎么做"

当用户想主动发消息（而非回复对方消息）时，**第一步永远是判断这条消息该不该发**，而不是直接帮他写。具体要做：

1. **动机审视**：用户想发这条消息，是出于真实的关心，还是为了缓解自己的焦虑和不确定感？结合 `self/patterns.md` 中的已知模式来判断。
2. **必要性评估**：上一条消息是谁发的？对方回了吗？如果用户是最后发消息的人且对方未回复，再次主动发消息的节奏是否合适？
3. **给出明确建议**：如果判断不该发，直接说"不发更好"并解释原因；如果该发，再进入"怎么发"的环节。不要跳过这一步直接帮用户。


## 流程

### 第一步：获取上下文

1. 执行 `python tools/wx_chat.py sync` 同步最新数据
2. 执行 `python tools/wx_chat.py chat <对方名字>` 拉取最近消息
3. 读取对方的人物档案（如 `crush/her.md`）了解性格特质和关系阶段
4. **比对并更新档案**：将最新消息与已沉淀的档案比对——时间线是否缺失新事件、关系阶段是否仍准确、对方状态信息是否有变化。如需更新，立即执行，不必等用户提醒

### 第二步：写入工作台

将分析结果写入 `workbench.md`，格式如下：

```markdown
# Workbench

## 时间背景

- **当前时间**：YYYY-MM-DD HH:MM（星期X）
- **对方最后消息**：HH:MM，距今 X 小时 X 分钟
- **我的最后消息**：HH:MM，距今 X 小时 X 分钟
- **对方当前状态**：（根据聊天内容推断，如"刚到校休息中"、"可能在复习"等）

## 最新消息

（粘贴最近几条关键消息）

## 意图分析

（对方这条消息的意图、情绪、潜台词。需说明时间因素如何影响判断）

## 策略方向

（应该怎么回应，以及为什么——只说方向和原因，不给具体话术）

---

请先写出你的回复，然后告诉我。

<details>
<summary>写完后点击查看 AI 最佳建议</summary>

**最佳回复**：...

**为什么这样回**：...（从对方性格、关系阶段、沟通目标角度解释）

</details>
```

### 第三步：等待用户回复

用户在对话中给出自己写的回复内容。

### 第四步：对比分析

用户给出回复后，AI 将分析结果**追加**到 `workbench.md` 底部：

```markdown
---

## 你的回复

（用户写的内容）

## 对比分析

（用户回复 vs AI 建议的核心差异，以及各自会产生什么效果）

## 洞察

（从用户的措辞和思路中观察到的心理模式和沟通倾向）

## 成长建议

（做得好的地方 + 可改进的点）
```

## 注意事项

- AI 给出的是基于已知信息的最佳判断
- 具体话术必须折叠隐藏在 `<details>` 中，保证用户先独立思考
- 分析时重点是帮助成长，而非评判好坏
- 如果用户的回复比 AI 建议更好，明确指出并说明为什么
- 每次新的消息触发时，`workbench.md` 会被覆盖重写为新一轮内容
