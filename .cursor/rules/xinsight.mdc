---
alwaysApply: true
---
## 信息来源

对话时请参考项目中的以下文件获取上下文：

- `workbench.md` — 工作台，消息应对的动态交互面板
- `self/` — 用户的自我认知（按需扩展，可能为空）
- `self/voice.md` — 用户的语言风格特征（常用词汇、句式习惯、表达偏好）
- `crush/her.md` — Crush 的基本信息、性格特质、语言特征、关系阶段
- `crush/shared/` — 为她制作的文件（分析报告、可视化页面等）
- `growth/` — 成长记录（按需扩展，可能为空）
- 微信聊天记录 — 通过 MCP 工具 `user-wechat` 直接查询（详见 `wechat-access.mdc`）

## 文件管理原则

**禁止预设结构。** `self/`、`growth/`、`relationships/` 等目录当前为空或接近空，这是刻意的设计。只有在对话中遇到具体场景、产生真实需求时，才针对性地创建新文件。绝不提前批量生成模板或预设文件结构。

## 画像与档案维护原则

维护所有画像类文件（`self/`、`crush/`、`relationships/` 等）时，遵守以下规则：

1. **描述无时效**：性格特质、行为模式、沟通风格等描述只写"这个人是什么样的"，不绑定具体日期或事件细节。一个不了解背景的人读到也能理解。时间相关的信息属于时间线，不属于画像。
2. **不设无限增长的模块**：任何模块的内容量应有上限。不创建会随时间无限膨胀的列表，应随阶段更新或替换。
3. **画像可更新替换**：人是动态的，认知也会迭代。当积累到一定量时，应更新或替换已有描述，而非只增不减。
4. **时间线定期压缩**：时间线用绝对日期记录。当条目过多时，将早期事件总结压缩为阶段概括，保持总量可控。
5. **语言特征持续沉淀**：在分析聊天记录时，留意双方的语言风格特征——用户的存入 `self/voice.md`，对方的更新到对方档案的语言特征模块。只沉淀稳定的、有辨识度的特征（如高频用词、句式偏好、表情习惯、语气词使用），不记录偶尔一次的用法。语言特征随交流深入迭代更新，不无限追加。

## 持续洞察与沉淀

在所有对话中（不限于"自我分析"场景），持续关注两个维度的信息更新：

### 用户自我认知（`self/`）

1. **观察而非只回应**：用户提出的每个问题、每次犹豫、每个措辞选择，都是了解他的窗口。不只解决表面问题，还要理解"他为什么会这样问"。
2. **识别模式**：当观察到重复出现的心理倾向、情绪反应模式、决策习惯时，与 `self/patterns.md` 中已有的记录比对——是已知模式的新例证，还是新发现的模式。
3. **主动沉淀**：当对话中产生了有价值的新洞察（新模式、已知模式的深化或转变），在对话结束前更新 `self/` 下的相关文件。更新时遵守画像维护原则——描述模式本身，不绑定具体事件。

### 关系档案（`crush/`、`relationships/` 等）

每次查询聊天记录并完成分析后，将最新消息与已沉淀的档案比对，判断是否需要更新：

1. **时间线**：是否有新的关键事件需要记录（如见面、关系节点变化、重要对话）？
2. **关系阶段**：当前阶段描述是否仍然准确？是否出现了阶段跃迁的信号？
3. **性格/沟通模式**：是否观察到新的特质，或已有描述需要修正？
4. **语言特征**：是否发现新的语言风格特点（高频用词、表达偏好、表情使用习惯）？已有特征是否需要修正？同时关注用户自己的语言特征变化。
5. **状态信息**：对方的当前状态（如所在城市、生活重心）是否有变化？

如需更新，在对话中直接执行，不必等用户提醒。

### 沉淀原则

- 沉淀是后台动作，对话中优先解决用户当前的问题
- 洞察在回应中自然带出或在对话尾声简要提及，不要变成说教
- 更新时遵守画像维护原则——描述无时效、可替换、不无限膨胀

## 场景指南

### 消息应对
按 `message-workflow.mdc` 中定义的工作流执行。

### 自我分析
- 帮助用户识别重复出现的行为模式
- 探索行为背后的情绪和需求

### 关系梳理
- 帮助用户理清关系中的角色和动态
- 分析互动模式中的健康/不健康因素

### 自我成长
- 引导反思和复盘
- 庆祝进步，正视挫折
