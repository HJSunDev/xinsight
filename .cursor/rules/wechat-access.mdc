---
description: 微信聊天记录访问指南 — 通过 MCP 工具直接查询微信消息
alwaysApply: true
---

# 微信聊天记录访问

## 接入方式

通过 MCP Server `user-wechat` 直接调用，无需执行命令行。数据实时从微信本地加密数据库解密读取，包含 WAL 增量数据。

## 前提条件

- 微信 PC 端（4.x）必须正在运行
- MCP Server 已在 Cursor 中配置并启动（`~/.cursor/mcp.json` 中的 `wechat` 条目）
- 底层工具：`E:\Dev\wechat-decrypt`（基于 wechat-decrypt，Python 3.12 虚拟环境）

## 可用工具

| 工具 | 用途 |
|------|------|
| `get_chat_history` | 获取指定联系人的聊天记录（支持昵称/备注模糊匹配、分页、时间范围筛选） |
| `get_recent_sessions` | 获取最近会话列表（最新消息摘要、未读数） |
| `get_contacts` | 搜索或列出联系人 |
| `search_messages` | 按关键词搜索聊天记录（可限定联系人、时间范围） |
| `get_new_messages` | 获取自上次调用以来的新消息 |
| `get_chat_images` | 列出某个聊天中的图片消息 |
| `decode_image` | 解密指定图片（需先通过 get_chat_history 获取 local_id） |

## 常用调用示例

```
# 查看某人最近消息
CallMcpTool(server="user-wechat", toolName="get_chat_history", arguments={"chat_name": "昵称或备注", "limit": 50})

# 按时间范围查询
CallMcpTool(server="user-wechat", toolName="get_chat_history", arguments={"chat_name": "昵称", "start_time": "2026-04-01", "end_time": "2026-04-03"})

# 搜索关键词
CallMcpTool(server="user-wechat", toolName="search_messages", arguments={"keyword": "关键词", "chat_name": "昵称"})

# 查看最近会话
CallMcpTool(server="user-wechat", toolName="get_recent_sessions", arguments={"limit": 20})
```

## 注意事项

- 语音消息返回的是元数据（时长、aeskey 等），无法直接转文字，需用户口述内容
- 图片消息显示 `local_id`，可用 `decode_image` 工具解密查看
- 数据直接从微信加密数据库实时解密读取，无需手动同步
- 如果 MCP Server 未启动或连接失败，检查微信是否在运行，必要时重启 Cursor

## AI 使用场景

当用户在对话中提到需要查看微信消息时：
1. 直接调用 `get_chat_history` 拉取对应联系人的消息
2. 基于消息内容进行分析、回复建议等
3. 不再需要执行 `sync` 命令，数据始终是最新的
